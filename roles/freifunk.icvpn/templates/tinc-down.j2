#!/bin/sh
/sbin/ip addr del dev $INTERFACE {{ bgp[icvpn_hostname].ipv4 }} broadcast 10.207.255.255
/sbin/ip -6 addr del dev $INTERFACE {{ bgp[icvpn_hostname].ipv6 }}
/sbin/ip link set dev $INTERFACE down

